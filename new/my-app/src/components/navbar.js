import React from 'react';
import ReactDOM from 'react-dom';

class reactNavbar extends React.Components {
  render() {
    <div id="navbar">
      <header>
        Test React Header
      </header>
    </div>
  }
}

export default reactNavbar
